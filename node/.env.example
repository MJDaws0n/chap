# Chap Node Environment Configuration
# Copy this file to .env and update with your credentials
#
# Get these values from your Chap dashboard:
#   1. Log in to your Chap server
#   2. Go to Nodes → Add Node
#   3. Copy the generated NODE_ID and NODE_TOKEN

# Unique identifier for this node (from Chap dashboard)
NODE_ID=

# Authentication token (from Chap dashboard)
NODE_TOKEN=

# WebSocket URL to your Chap server
# Format: ws://your-server-ip-or-domain:8081
CHAP_SERVER_URL=ws://your-chap-server:8081

# Browser WebSocket Server (for live logs/deployment streams)
# This runs on the node and browsers connect directly to it
BROWSER_WS_PORT=6002
BROWSER_WS_HOST=0.0.0.0

# For production with SSL, provide cert paths:
# BROWSER_WS_SSL_CERT=/path/to/cert.pem
# BROWSER_WS_SSL_KEY=/path/to/key.pem
